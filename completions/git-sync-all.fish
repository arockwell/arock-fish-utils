# Fish completions for git-sync-all

# Options
complete -c git-sync-all -s f -l fetch-only -d "Only fetch, don't pull"
complete -c git-sync-all -s a -l all -d "Show all worktrees including clean ones"
complete -c git-sync-all -s h -l help -d "Show help message"
